<% if @line_sizing.errors.any?%>
	$(".error_messages").remove();
	$(".edit_line_sizing").prepend("<%= escape_javascript(error_messages_for(@line_sizing))%>");
	$.colorbox.resize();
<% else%>   
	alert("Updated Line sizing Successfully.");
	window.location.reload();
<% end%>
