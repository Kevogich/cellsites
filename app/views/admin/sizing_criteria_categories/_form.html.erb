<%= form_for sizing_criteria_category, :url => sizing_criteria_category.new_record? ? admin_sizing_criteria_categories_path : admin_sizing_criteria_category_path(sizing_criteria_category), :remote=>true,:html => {:style => 'width:400px;'} do |f| %>
	<%= f.error_messages %>	
	<%= f.hidden_field :company_id%>
	<div class="form-element-last">
		<%= f.label :name%><br>
		<%= f.text_field :name%>
	</div>
	<div class="form-buttons">
		<%= f.submit sizing_criteria_category.new_record? ? 'Create' : 'Update'%>		
  	</div>
<% end%>